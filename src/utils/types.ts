export type artistType = {
  name: string,
  image: string, 
  followers: number,
  albums: string [],
  genres: string [],
  listenOnSpotify: string,
  id: string
}

export type albumType = {
  link: string[],
}